用户使用PyMake发现type命令没有system、local、custom的支援，
我会逐渐加上这块支援。

我建议用户，在自己编写的json储存的脚本里引用环境里的变量不要使用${}，仅仅使用%ENVVAR%(Windows)、$ENVVAR(Unix*)，
我做了这样的假设，我假设用户会按照我预期的规格编写脚本，所以初步没有提供以上基本环境的支持，
而且，最初开发的时候，只是为孤立的、冲突的环境服务，也顾及不到通用的环境，
后续加入。


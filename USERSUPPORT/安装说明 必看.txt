有的用户口味比较挑剔，对命令的名字有自己的喜好，在USERSUPPORT里，我教给用户如何安装自己的命令。

Windows （需要管理员权限）
使用 pyinstall %* 来安装。使用 pyuninstall 来卸载。

*Unix （需要sudo）
使用 pyinstall.sh 来安装。使用 pyuninstall.sh 来卸载。

使用：
我把程序安装为pyenv命令，所以终端里用pyenv命令调用。
可以在任何地方调用。
